node_modules/
database/screenshots/
database/logs/
projects/*/video-frames/
projects/*/screenshots/
*.png
# Keep per-business hero/logo images so they're reused (not re-billed) on each pipeline run
!projects/*/hero.png
!projects/*/logo.png
*.mp4
# Keep per-business promo videos so they deploy to GitHub Pages
!projects/*/promo.mp4
.env
